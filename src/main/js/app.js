import React from 'react';
import ReactDOM from 'react-dom';
import axios from 'axios';


var root = document.getElementById('react');
ReactDOM.render(
//  <MainPanel {...(root.dataset)}/>,
<label> ciaone </label>,
  root
);